{% extends 'dashboard.html.twig' %}

{% block title %} {% trans %} Message {% endtrans %}{% endblock %}

{% block body %}
<div class="container">
		<div class="row">
			<div class="col-xl-5 offset-xl-3">

				<div
					class="login-register-page">
					<!-- Welcome Text -->
					<div class="welcome-text">
						<h3 style="font-size: 26px;"> {% trans %}Let's send a Message {% endtrans %}</h3>
						
					</div>

					{{ form_start(msg) }}

					
				
					 {# {{ form_widget(registrationForm.roles	)}}  #}
					{{ form_widget(msg.Message,{'attr':{'type':'text','class':'form-control','placeholder':'Message...'}}) }}
					
					

					<div class="submit-field"></div>
				</div>
				<button type="submit" class="button full-width button-sliding-icon ripple-effect margin-top-10"> {% trans %} Send {% endtrans %}<i class="icon-material-outline-arrow-right-alt"></i>
				</button>
				{{ form_end(msg) }}

			</div>

		</div>
	</div>
{% endblock %}
